<h1>Listing startups</h1>

<table>
  <thead>
    <tr>
      <th>Company name</th>
      <th>Year founded</th>
      <th>Description</th>
      <th>Milestones</th>
      <th>Funding</th>
      <th>Press</th>
      <th>Category</th>
      <th>People</th>
      <th>Advisors</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @startups.each do |startup| %>
      <tr>
        <td><%= startup.company_name %></td>
        <td><%= startup.year_founded %></td>
        <td><%= startup.description %></td>
        <td><%= startup.milestones %></td>
        <td><%= startup.funding %></td>
        <td><%= startup.press %></td>
        <td><%= startup.category %></td>
        <td><%= startup.people %></td>
        <td><%= startup.advisors %></td>
        <td><%= link_to 'Show', startup %></td>
        <td><%= link_to 'Edit', edit_startup_path(startup) %></td>
        <td><%= link_to 'Destroy', startup, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Startup', new_startup_path %>
